name: watchface_ai
description: "AI-powered Wear OS watch face builder with Gemini AI integration."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.0.0
  flutter_animate: ^4.0.0
  google_generative_ai: ^0.4.0
  http: ^1.0.0
  shared_preferences: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
